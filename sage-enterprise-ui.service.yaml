apiVersion: v1
kind: Service
metadata:
  name: sage-enterprise-ui
  namespace: sage-ui
spec:
  selector:
    app: sage-enterprise-ui
  ports:
    - name: http
      port: 80
      targetPort: 3000
  type: ClusterIP
