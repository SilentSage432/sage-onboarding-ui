"use client";

// Enterprise mode active — account selection disabled.
// This component is kept for import compatibility but will never be used in enterprise flow.
const AccountTypeStep = () => {
  return (
    <div className="text-center text-white opacity-60">
      Enterprise mode active — account selection disabled.
    </div>
  );
};

export default AccountTypeStep;
