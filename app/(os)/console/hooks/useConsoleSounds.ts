export function useConsoleSounds() {
  // placeholder for future audio hooks
  // examples:
  // play("console_open")
  // play("panel_focus")
  // play("hadra_activation")
  // play("agent_selection")
  
  return {
    play: (name: string) => {
      // Future implementation will use Web Audio API or HTML5 Audio
      // For now, this is a no-op placeholder
    },
  };
}

